{{#if session.isAuthenticated}}
  {{outlet}}
{{else}}
  <div class="row">
    <div class="col-md-12 text-center">
      <img alt="SMART" src="assets/images/cm_logo_large.png">
    </div>
    <div class="col-md-12 text-center">
      {{#link-to 'login' class="btn btn-info col-md-12"}}LOGIN{{/link-to}}
    </div>
  </div>
{{/if}}

